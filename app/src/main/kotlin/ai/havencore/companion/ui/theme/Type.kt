package ai.havencore.companion.ui.theme

import androidx.compose.material3.Typography

/**
 * Stock Material 3 Typography. To customize the font, build a
 * `FontFamily` (e.g. via `androidx.compose:ui-text-google-fonts`)
 * and pass it through `Typography(...)` here. One file, no screen
 * edits. See `docs/design-system.md`.
 */
val HavenTypography: Typography = Typography()
